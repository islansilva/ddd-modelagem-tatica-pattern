import OrderItem from "../entity/order_item"

describe("Order Service unit tests", () => {
    it("should get total of all orders", () => {

        const orderItem = new OrderItem("i1", "prod 1")

    })
})